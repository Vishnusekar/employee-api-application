apiVersion: v1
kind: Service
metadata:
  name: {{ include "employee-platform.fullname" . }}
  labels:
    {{- include "employee-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "employee-platform.selectorLabels" . | nindent 4 }}
