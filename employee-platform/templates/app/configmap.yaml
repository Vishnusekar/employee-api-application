apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-config" (include "employee-platform.fullname" .) }}
  labels:
    {{- include "employee-platform.labels" (dict "root" . "component" "api") | nindent 4 }}
data:
  APP_NAME: {{ .Values.config.appName | quote }}
  ENVIRONMENT: {{ .Values.config.environment | quote }}
  STARTUP_DELAY: {{ .Values.config.startupDelay | quote }}
  FAIL_AFTER: {{ .Values.config.failAfter | quote }}
  DATABASE_HOST: {{ include "employee-platform.postgres.service.fullname" . | quote }}
  DATABASE_PORT: {{ .Values.config.databasePort | quote }}
  DATABASE_NAME: {{ .Values.config.databaseName | quote }}