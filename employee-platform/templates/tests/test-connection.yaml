apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "employee-platform.fullname" . }}-test-connection"
  labels:
    {{- include "employee-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "employee-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
