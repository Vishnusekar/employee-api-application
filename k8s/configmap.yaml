apiVersion: v1
kind: ConfigMap
metadata:
  name: employee-api-config
  namespace: employee
data:
  APP_NAME: "Employee API"
  ENVIRONMENT: "QA"
  STARTUP_DELAY: "30"
  FAIL_AFTER: "0"
  DATABASE_HOST: postgres-service
  DATABASE_PORT: "5432"
  DATABASE_NAME: employee